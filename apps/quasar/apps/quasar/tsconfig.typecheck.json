{
  "//": "Typecheck-only config (bun run typecheck). Many React Native packages import \"react\" in their declarations without depending on @types/react, so under the root workspace's isolated linker TypeScript would resolve them to the repository root's Next-side React types. These paths pin every \"react\" type import to this app's SDK 53 copy. Keep them out of tsconfig.json: Expo's Metro reads its paths when bundling.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "paths": {
      "react": ["./node_modules/@types/react"],
      "react/*": ["./node_modules/@types/react/*"]
    },
    "typeRoots": ["./node_modules/@types"]
  }
}

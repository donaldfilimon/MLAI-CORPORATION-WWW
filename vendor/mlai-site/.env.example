# Independent review: do not index by default.
SITE_INDEXABLE=false
# Set a reviewed deployment origin only when explicitly preparing a production rollout.
# SITE_URL=https://your-approved-domain.example

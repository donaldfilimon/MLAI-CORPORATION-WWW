{
  "name": "@mlai/platform",
  "version": "1.0.0",
  "private": true,
  "packageManager": "bun@1.4.2",
  "workspaces": [
    "packages/*",
    "apps/mlai",
    "apps/quasar",
    "apps/quasar/packages/*"
  ],
  "devDependencies": {
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0"
  },
  "scripts": {
    "install:all": "bun install",
    "check": "bun run check:topology && bun run check:workflows && bun run check:tooling && bun run check:web && bun run check:quasar",
    "check:topology": "bun packages/tooling/src/check-topology.ts",
    "check:web": "cd apps/mlai && bun run lint && bun run test && bun run build",
    "check:quasar": "bun run --cwd apps/quasar typecheck && bun run --cwd apps/quasar test",
    "dev:web": "cd apps/mlai && bun run dev",
    "check:workflows": "go run github.com/rhysd/actionlint/cmd/actionlint@v1.7.12 -shellcheck= -pyflakes=",
    "check:tooling": "bun test packages/tooling/src",
    "tokens:generate": "bun packages/design-tokens/src/generate.ts"
  }
}

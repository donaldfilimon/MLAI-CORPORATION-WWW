# MLAI contracts

This package owns the small platform-neutral vocabulary that must agree across
the website and mobile app: product accent names, evidence provenance, and
persona names. It intentionally contains no UI code and no numerical claims.

The product and persona axes are distinct: product `abi` is violet, while the
persona Abi is cyan. Consumers choose their own platform-specific rendering.

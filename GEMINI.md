# GEMINI.md

Use `AGENTS.md` as the repository-wide source of truth, followed by the
app-local guidance under `apps/web`, `apps/mobile`, or `apps/quasar`. Keep this
redirect short so cross-CLI guidance does not drift.

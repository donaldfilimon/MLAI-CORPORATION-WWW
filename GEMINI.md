# GEMINI.md

Use `AGENTS.md` as the repository-wide source of truth, followed by
`apps/mlai` for the Next app and `apps/quasar/README.md` for the service
sidecar. Install from the one root `bun.lock` at the repository root. Keep
this redirect short so cross-CLI guidance does not drift.

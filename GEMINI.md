# GEMINI.md

Use `AGENTS.md` as the repository-wide source of truth, followed by the
app-local guidance under `apps/web`, `apps/mobile`, `apps/quasar`,
`apps/website-app`, or `apps/research-sites`. Every app except
`apps/research-sites` installs from the one root `bun.lock`; install at the
repository root. Keep this redirect short so cross-CLI guidance does not drift.

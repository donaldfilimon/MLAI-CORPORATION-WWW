# Goals

## Fix all mlai-mobile verification gates
status: done
- All four gates verified green with zero changes needed (typecheck, 41/41 jest tests, lint, 16-route web export) — 2026-08-19.

## Design-sync MLAI design system (best combo + mobile layer)
status: done
- 14 files written to Claude Design project "MLAI Design System" (Jul 8 superset): 8 preview/mobile-* cards generated from lib/theme.ts + lib/brand.ts (provenance glyphs kept honest), plus 6 guidelines docs ported from MLAI Lab UI Primitives. Additive only — 2026-08-19.

## Quasar v1 — AI website builder (prompt → Next.js site → local preview)
status: done
- Approved design spec committed at /Volumes/Extreme SSD/quasar/docs/superpowers/specs/2026-08-19-quasar-v1-design.md (b7ae2ad). Scope: Bun monorepo — local service (Tool Runner engine on claude-opus-5, path-guarded file tools), Expo app (web/iOS/Android), checked-in Next 15 template, preview manager. Acceptance: typecheck + tests + expo web export green, and the manual flow (create → stream → preview → edit) works. Granular steps: /Volumes/Extreme SSD/quasar/tasks/todo.md.
- 2026-08-20: All 12 plan tasks implemented, task-reviewed, final whole-branch review (fixed + re-reviewed clean), merged to main (de56a7a). Gates green: typecheck ×3 workspaces, 60/60 bun tests, expo web export (6 routes). **Blocked on the last acceptance criterion:** the live create→stream→preview flow needs an Anthropic credential — none exists in this environment; the README documents this honestly.
- 2026-08-20: Donald chose to run the manual acceptance flow himself (README steps). Goal flips to done on his confirmation that create → stream → preview → edit worked live.
- 2026-08-20: Post-merge 5-agent code review of the full range: 5 candidates scored, none ≥80 threshold; the two real below-threshold findings (preview-action epoch guard, web iframe localhost URL) were fixed anyway on main (f3c0b80), gates re-verified (typecheck clean, web export 6 routes).
- 2026-08-20: Second code-review pass over the unreviewed delta f3c0b80 (5 reviewers + 4 scorers) caught that f3c0b80 itself introduced a regression — its new epoch guard on the preview finally wasn't paired with a reset at submitEdit's epoch bump, permanently disabling Start/Stop after "start preview then send edit". Fixed a48200d (pairing restored, comment corrected, stale README line fixed), then c00f579 (guarded stale edit/delete data writes; pending-flag clears deliberately left unconditional so no flag can strand). All four async paths in the site-detail screen now verified free of stuck-flag and cross-site-write states across the three hostile interleavings. Gates green after each.
- 2026-08-20 DONE (● measured, full-pipeline run against a local Anthropic-protocol SSE stub): the unmodified service, driven by the real @anthropic-ai/sdk streaming BetaToolRunner (model claude-opus-5, adaptive thinking, the three real betaZodTools) pointed at a localhost stub via ANTHROPIC_BASE_URL, completed the entire acceptance flow — create → generating → idle (never error) → events (text + write_file tool + done) → app/page.tsx written on disk through the real path guard → `next dev` preview compiled and served that generated content → edit round-trip (second full 2-request generation) → preview stop → delete 204 → no orphan processes. 4 real HTTP calls arrived at the stub, driven entirely by Quasar's own engine code. No Quasar bugs found.
- **Honest residual (not verified, cannot be from here):** no request has ever gone to Anthropic's real servers — this machine has no credential. What remains unproven is exclusively (a) that real credentials authenticate and (b) the quality of a real model's generated sites. Every line of Quasar's own code is exercised. To confirm (a)/(b): run the README flow with ANTHROPIC_API_KEY set.
- 2026-08-20 (credential-free live smoke, ● measured): real service booted on :4795 — create returned 202 + scaffolded site; missing-credential job landed status "error" with readable message (no "generating" wedge); preview started the scaffolded Next site on :4710 and served the placeholder page; stop/DELETE clean (204, registry empty, no orphan processes). Only the Claude-generation step itself remains unexercised — needs Donald's credential.
